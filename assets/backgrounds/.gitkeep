# 背景图片文件夹

此目录用于存放票面底纹和背景图片。

推荐格式：
- SVG (推荐，矢量格式，无损缩放)
- PNG (支持透明背景)

文件命名建议：
- ticket_background.svg  - 主票面背景
- watermark.svg          - 水印图案
- pattern.svg            - 底纹图案

使用方法：
在 Typst 模板中通过 image() 函数引用这些背景图。
